# Repo-local pnpm configuration
#
# This repo standardises on pnpm (no npm/npx).
# Keep pnpm's content-addressable store inside .pixi/ so it's local and
# automatically gitignored.
store-dir=.pixi/pnpm-store

# Reduce noise for CI/dev shells
fund=false
audit=false
