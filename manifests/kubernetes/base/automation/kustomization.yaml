# Automation Services Kustomization
# Workflow automation and policy enforcement
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: flexstack-automation

namespace: flexstack

commonLabels:
  app.kubernetes.io/part-of: flexstack
  app.kubernetes.io/managed-by: kustomize
  flexstack.io/profile: automation

resources:
  - n8n.yaml
  - opa.yaml
